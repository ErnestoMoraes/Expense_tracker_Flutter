import 'package:flutter/material.dart';

class ExpenseSumary extends StatelessWidget {
  const ExpenseSumary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
